# cephfs-pvc.yaml — CephFS 共享存储 PVC 示例
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cephfs-pvc
spec:
  accessModes:
    - ReadWriteMany           # CephFS 支持多节点同时读写
  storageClassName: ceph-cephfs
  resources:
    requests:
      storage: 100Gi
