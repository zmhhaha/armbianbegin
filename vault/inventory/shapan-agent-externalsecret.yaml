# Optional ShaPan model-provider credentials.
# Example Vault path: secret/shapan/agent
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: shapan-agent
  namespace: shapan
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: shapan-agent
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: secret/data/shapan/agent
