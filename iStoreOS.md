在 iStoreOS 上配置代理主要分三步：**①安装代理插件**、**②配置订阅节点**、**③设置网络模式**。

你可以根据软路由在网络中的角色，选择下面两种方案之一。

### ⚙️ 准备工作：换软件源，为安装提速

在开始前，强烈建议先更换软件源，可以避免因网络问题导致安装失败。

1.  登录 iStoreOS 管理后台，在**首页**找到并点击 **“软件源配置”**。
2.  在弹出的窗口中，将软件源切换为 **“阿里云”**，然后点击确认保存。

### 方案一：iStoreOS 作为旁路由 (最推荐)

这个方案对现有网络影响最小，只需将 iStoreOS 连接到主路由，配置好代理后，其他设备只要把“网关”指向它，就能自动实现“透明代理”。

**1. 安装代理插件**
*   iStoreOS 的应用商店（iStore）可能没有你需要的代理插件。
*   你可以从第三方获取插件的 `.run` 安装包，例如在 GitHub 上 `AUK9527/Are-u-ok` 项目里可以找到。
*   下载后，在 iStore 后台点击 **“iStore” -> “手动安装”**，上传 `.run` 文件即可自动安装。常见的插件有 `PassWall`、`OpenClash`、`SSR Plus`。

**2. 配置代理插件 (以 PassWall 为例)**
*   安装成功后，在 **“服务”** 菜单下找到并进入 **PassWall**。
*   在 **“节点订阅”** 最下方，**添加**你的代理订阅地址，然后点击 **“保存&应用”**。
*   最后，点击 **“手动订阅”** 来更新节点列表。

**3. 设置为旁路由模式**
*   在 iStoreOS 后台，进入 **“网络” -> “接口”**，编辑 **LAN** 接口。
*   将 **“IPv4 地址”** 设置为一个与主路由同网段的静态 IP（例如，主路由是 `192.168.1.1`，可以设为 `192.168.1.2`）。
*   将 **“IPv4 网关”** 设置为主路由的 IP 地址。
*   在 **“DHCP 服务器”** 标签页中，**取消勾选 “忽略此接口”**，或者直接**关闭 DHCP 服务**。这一步是为了避免和主路由的 DHCP 服务冲突。

**4. 修改防火墙（如需要）**
*   如果配置后无法上网，可以检查防火墙设置。进入 **“网络” -> “防火墙”**，在 **“常规设置”** 中，将 **“入站数据”**、**“出站数据”** 和 **“转发”** 都设置为 **“接受”**。
*   也可以在 **“自定义规则”** 中添加一行：`iptables -t nat -I POSTROUTING -o eth0 -j MASQUERADE`。

**5. 客户端设备指向旁路由**
*   **手动设置**：在需要代理的设备（如电脑、手机）上，将网络连接的“网关”和“DNS”服务器手动设置为 iStoreOS 的 IP 地址（如 `192.168.1.2`）。
*   **自动分发（推荐）**：在主路由的 DHCP 设置中，将“默认网关”和“首选 DNS”都修改为 iStoreOS 的 IP 地址。这样，所有设备都会自动通过 iStoreOS 上网。

### 方案二：iStoreOS 作为主路由

如果你的 iStoreOS 本身就是主路由，那配置更简单，因为它直接管理所有网络流量。

1.  **安装和配置代理插件**：参考方案一的 **步骤 1** 和 **步骤 2**。
2.  **无需额外网络设置**：由于 iStoreOS 本身就是网关，代理插件生效后，默认就能接管所有流量。
3.  **配置防火墙**：同样建议参考方案一 **步骤 4** 进行防火墙设置，避免因防火墙拦截导致代理失效。

### 💡 一些额外提示

*   **避免插件冲突**：注意不要同时安装 `PassWall` 和 `SSR Plus` 等多个代理插件，它们可能会冲突。
*   **指定设备走代理**：如果只想让特定设备走代理，可以在代理插件的 **“访问控制”** 或 **“规则管理”** 中，指定这些设备的 IP 或 MAC 地址。
*   **为 Docker 设置代理**：如果需要解决 Docker 镜像拉取问题，可以安装 **“iStore增强”** 插件，它会自动配置 Docker 镜像代理。
*   **检查系统时间**：确保 iStoreOS 的系统时间是准确的，时间不同步可能导致 TLS 握手失败。

配置完成后，回到你的 `arm-cluster-master` 服务器，再次执行 `curl -I https://github.com/login/oauth/authorize`，如果不再超时，就说明代理配置成功了。

只代理 GitHub 流量，这个需求在 iStoreOS 上完全可以实现。核心思路就是**修改代理插件的分流规则，只让 GitHub 的流量走代理，其他所有流量都直连**。

针对你正在使用的 iStoreOS，这里有两种主流插件的配置方法，你可以根据情况选择。

### 🔧 方法一：使用 PassWall (更推荐，设置简单)

如果你使用的是 `PassWall` 插件，这是最直接的方法。

1.  **进入规则管理**：在 iStoreOS 后台的“服务”菜单中找到并进入 `PassWall`，然后点击“规则管理”。
2.  **添加代理域名**：在“规则管理”页面，找到**“代理列表”**（或“直连列表”的相反设置），点击“添加”按钮。
3.  **填入 GitHub 域名**：在弹出的窗口中，填写规则名称（如 `GitHub`），并在“域名”输入框中填入 `github.com`。
4.  **保存并应用**：点击保存，然后**务必把这条新规则移动到列表的最上方**，因为规则匹配是从上到下执行的，优先级最高。
5.  **设置默认模式**：在 `PassWall` 的“主要”设置页面，将“TCP节点”和“UDP节点”选为你的代理节点，并将**“默认代理模式”设置为“直连”**。

**这样设置后，流量走向会是：** 所有流量默认直连（不走代理） -> 规则匹配到 `github.com` -> 走代理节点。

> **小提示**：如果 `git clone` 等操作仍不成功，可以尝试在“代理列表”中再添加一条 `*.github.com` 或 `github.com` 的泛域名规则。同时，确保 PassWall 的 DNS 设置正确，避免因 DNS 解析导致分流失效。

---

### ⚙️ 方法二：使用 OpenClash (更灵活，需编辑配置)

`OpenClash` 基于 Clash 内核，配置更灵活，但需要手动编辑或上传配置文件。

1.  **进入配置文件管理**：在 iStoreOS 后台的“服务”菜单中找到 `OpenClash`，进入“配置文件管理”。
2.  **编辑或创建规则**：你可以直接编辑现有配置文件，或创建一个新的。在 `rules:` 部分，**在最上方**添加以下规则：
    ```
    rules:
      # 新增规则：让 GitHub 走代理
      - DOMAIN-SUFFIX,github.com,你的代理节点名称
      # 原有规则...
    ```
    其中，`你的代理节点名称` 需要替换成你在 OpenClash 中设置的节点名称。
3.  **设置默认直连**：在配置文件的最底部，**确保最后一条规则是直连所有流量**：
    ```
      - MATCH,DIRECT
    ```
4.  **应用配置**：保存配置文件后，在 `OpenClash` 的“配置文件管理”页面点击“切换”来应用它。

**这个配置的逻辑是：** 所有流量默认直连 -> 规则匹配到 `github.com` -> 走代理节点。

---

### 💡 其他选择：使用 MosDNS 或 GProxy

*   **使用 MosDNS**：如果你安装了 `MosDNS` 插件，也可以通过它的“规则列表”来实现。在 `MosDNS` 的“白名单”或自定义规则中，添加 `github.com` 并将其指向代理服务器。但它的主要功能是 DNS 分流，最终的网络流量仍需配合代理插件使用。
*   **使用 GProxy 工具**：另外，社区有一个叫 `GProxy` 的轻量级工具，可以专门为 GitHub 相关的 `git clone`、`docker pull` 等命令创建透明的 SOCKS5 代理。如果只想解决命令行工具的网络问题，这也是一个选择。

### ⚠️ 注意事项

*   **规则优先级**：在 `PassWall` 中，务必确保新规则置顶；在 `OpenClash` 中，确保规则在 `rules` 列表的最上方。优先级错误会导致规则不生效。
*   **节点连通性**：在开始配置前，先确保你的代理节点本身是正常可用的。
*   **代理模式**：在 `OpenClash` 中，建议将“代理模式”设置为 `Rule (规则)` 模式，而不是 `Global (全局)` 模式。
*   **故障排查**：如果配置后 `curl -I https://github.com` 仍然超时，可以检查一下 `PassWall` 或 `OpenClash` 的日志，看看 `github.com` 的流量是否真的被匹配并转发到了代理节点。

建议你先从 **PassWall** 的方法开始尝试，它的图形界面操作更直观，更容易上手。